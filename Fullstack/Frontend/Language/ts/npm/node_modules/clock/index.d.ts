export interface Clock {
    /**
     * Returns the timestamp in milliseconds since the Unix epoch
     *
     */
    now(): number;
}
export declare const systemClock: Clock;
export default systemClock;
export declare function constantClock(at?: number): Clock;
